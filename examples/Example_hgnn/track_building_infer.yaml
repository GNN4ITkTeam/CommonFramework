# CommonFramework configuration
stage: track_building
model: ConnectedComponents
input_dir: /global/cfs/cdirs/m2616/pmtuan/inferred_data/igcn_14999421_epoch66_redo/ # Should contain the files used in training and inference
stage_dir: /global/cfs/cdirs/m3443/usr/ryanliu/HGNN/connected_components/ # A directory to be created that will store logs, artifacts, and output data

# Dataset parameters
data_split: [100, 100, 100] # Number of [training, validation, testing] examples
score_cut: 0.8