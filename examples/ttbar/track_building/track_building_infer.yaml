# CommonFramework configuration
stage: track_building
model: CCandWalk
input_dir: /global/cfs/cdirs/m2616/pmtuan/inferred_data/ignn2_18036494_epoch78 # Should contain the files used in training and inference
stage_dir: /global/cfs/cdirs/m2616/pmtuan/inferred_data/ignn2_18036494_epoch78_track_candidate_check # A directory to be created that will store logs, artifacts, and output data

# Dataset parameters
data_split: [1, 1000, 1000] # Number of [training, validation, testing] examples

# Connected Components and Walkthrough thresholds
score_cut_cc: 0.01
score_cut_walk: 
  min: 0.1
  add: 0.6

max_workers: 64

log_level: INFO