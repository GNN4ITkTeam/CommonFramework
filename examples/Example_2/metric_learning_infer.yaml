# CommonFramework configuration
stage: graph_construction
model: MetricLearning
input_dir: /mnt/data0/dittmeier/data/gnn4itk/feature_store/ # Should contain the files used in training and inference
stage_dir: /mnt/data0/dittmeier/data/gnn4itk/CF_reference_weighting/ # A directory to be created that will store logs, artifacts, and output data
project: CF_reference_weighting # Used in logging
gpus: 1
nodes: 1
num_workers: [0, 0, 0]

# Dataset parameters
data_split: [80, 10, 10] # Number of [training, validation, testing] examples

# Model inference parameters
r_infer: 0.1
knn_infer: 500
