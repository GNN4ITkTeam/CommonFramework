stage: edge_classifier
model: InteractionGNN
input_dir: MY_DATA_DIR/Example_2/metric_learning/ # Should contain the files used in training and inference
stage_dir: MY_DATA_DIR/Example_2/gnn/ # A directory to be created that will store logs, artifacts, and output data
project: CF_Example_2_GNN # Used in logging
gpus: 1
nodes: 1